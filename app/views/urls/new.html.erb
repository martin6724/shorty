
<h1>Fun-Sized URL</h1>
<h2>Short is a good thing when it comes to urls...</h2>

<%= image_tag("shorty_bootstrap.jpg", :alt => "gerbils are cute, like the url that we're about to generate.") %>
<% @url.errors.each do |attr, msg| %>
<%= msg %> on <%= attr %>
<% end %>

<%= form_for @url do |f| %>
<p>
<%= f.label :link %> : <%= f.text_field :originalurl %>
</p>
<p>
<%= f.hidden_field :random_id, :value => @url.random_id %>
<%= f.submit "Minify responsibly, shorty! :) " %>
</p>
<% end %>
